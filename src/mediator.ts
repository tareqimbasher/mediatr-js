export class Mediator {
    public sum(a: number, b: number): number {
        return a + b;
    }

    public sum2(a: number, b: number): number {
        return a + b;
    }

    public sum3(a: number, b: number): number {
        return a + b;
    }
}